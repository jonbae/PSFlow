
bower_components/
node_modules/
.pulp-cache/
output/
output-es/
generated-docs/
.psc-package/
.psc*
.purs*
.psa*
.spago

/xyflow-main
/xyflow

# Layer 0 intermediate extracts (regenerated by `npm run parity:api`).
# The committed snapshot is parity/layer0-api/report.md; the oracle bundle
# (oracle/index.js) is committed so `spago test` works without xyflow/.
parity/layer0-api/upstream.json
parity/layer0-api/psflow.json

