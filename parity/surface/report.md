# Surface parity — export, shape, behavior and prop-member report

_Generated by `npm run parity:surface` — do not edit by hand. Last run: 2026-08-31._

Upstream: `@xyflow/react` **12.11.0** / `@xyflow/system` **0.0.77**.
PSFlow was originally ported against `@xyflow/react@^12.3.5`. Every changelog
entry between that floor and the baseline above has since been bucketed by the
12.3.5→12.11.0 drift audit (ticket 071), so a divergence below is a genuine
port gap, not unclassified version drift — see
[`parity/changelog-audit/report.md`](../changelog-audit/report.md).

Method: names and prop members are extracted from upstream via the TypeScript
compiler API (resolves `export *`, renames, type-only exports) and *shapes* by
bundling the same vendored entry point and importing it — `forwardRef` and
`memo` are calls, not declarations. PSFlow's values come from **importing**
`index.js`, which is why this gate needs `spago build` first and is no longer
standalone; its type names still come from `src/React.purs`, types having no
runtime existence to import.

The same two imported barrels supply the behavioral checks: frozen snapshots
of the eight enum objects are deep-compared, then seventeen pure functions are
called with cloned copies of one shared input each and their returns compared.
This is still a seconds-only Node gate; it mounts nothing and needs no browser.

An upstream **value** is satisfied only by a PSFlow **value**. The two name
sets were merged until ticket 041, which passed nine audience-reaching absences
by construction: `import { Position } from 'ps-flow'` was `undefined` while
this report called it present. The *extra* bucket keeps the union, because it
asks a different question — whether PSFlow publishes a name upstream does not
have at all.

**Every allowlist entry below is live.** An entry that no longer corresponds to
a real difference **fails as stale**, permanent entries included — the
allowlist is the second implementation of a **region** (`CONTEXT.md`), and the
rule is the same on both: a register bites when its entries stop being true,
instead of accumulating silently until a real divergence lands on one and
passes.

## Top-level exports

- upstream: 210 names (68 value + 142 type)
- PSFlow: 224 names (69 value + 163 type)

### ❌ Missing in PSFlow (parity gaps — gates CI)

_none — full parity (modulo the allowlist below)._

### ➕ Extra in PSFlow (extensions / PS-idiom — informational)

- `BezierPathParams`
- `ConnectionClickStartHandle`
- `ConnectionInProgressData`
- `ConnectionLineProps`
- `ConnectionStatus`
- `EdgesStateBundle`
- `FlowExportObject`
- `KeyMatcher`
- `MiddlewareKey`
- `MiniMapNodesProps`
- `NodeOrIdOrRect`
- `NodeRefForBounds`
- `NodeResizeControlProps`
- `NodesEdgesState`
- `NodesStateBundle`
- `OnBeforeDeleteResult`
- `OnReconnectEnd`
- `OnViewportChange`
- `ReactFlowWithRef`
- `ReconnectHandleType`
- `ResizeObserver`
- `ScreenToFlowOptions`
- `SmoothStepPathParams`
- `StraightPathParams`
- `Style`
- `UpdateOptions`
- `UseKeyPressOptions`
- `ViewportPortalProps`
- `ZoomOptions`

### 📋 Known divergences (allowlisted)

**Intentionally absent from PSFlow:**

| Symbol | Rationale |
|--------|-----------|
| `BuiltInEdge` | Upstream union of the four built-in edge variants, discriminated by a string-literal `type`. PS `Edge e` carries `type :: Maybe String`, so the variants are not distinct types to union — documented in src/React.purs's header. Intentional and final. |
| `BuiltInNode` | Upstream union of the built-in node variants, discriminated by a string-literal `type`. As `BuiltInEdge`: PS `Node n` carries `type :: Maybe String` — documented in src/React.purs's header. Intentional and final. |
| `ControlProps` | Renamed `ControlsProps` in PSFlow (see extras). |
| `EdgeAddChange` | Constructor of the PS `EdgeChange` sum type, reachable via `EdgeChange(..)`. |
| `EdgeRemoveChange` | Constructor of the PS `EdgeChange` sum type, reachable via `EdgeChange(..)`. |
| `EdgeReplaceChange` | Constructor of the PS `EdgeChange` sum type, reachable via `EdgeChange(..)`. |
| `EdgeSelectionChange` | Constructor of the PS `EdgeChange` sum type, reachable via `EdgeChange(..)`. |
| `EdgeTypes` | Renamed `EdgeTypesMap` in PSFlow (see extras). |
| `NoConnection` | Nullary constructor of PS `ConnectionState`, not a standalone type — documented in src/React.purs header & ticket 054. |
| `NodeAddChange` | Constructor of the PS `NodeChange` sum type, reachable via `NodeChange(..)`. TS exports each change as a standalone type; PS models them as one sum type (intentional). |
| `NodeDimensionChange` | Constructor of the PS `NodeChange` sum type, reachable via `NodeChange(..)`. |
| `NodePositionChange` | Constructor of the PS `NodeChange` sum type, reachable via `NodeChange(..)`. |
| `NodeRemoveChange` | Constructor of the PS `NodeChange` sum type, reachable via `NodeChange(..)`. |
| `NodeReplaceChange` | Constructor of the PS `NodeChange` sum type, reachable via `NodeChange(..)`. |
| `NodeSelectionChange` | Constructor of the PS `NodeChange` sum type, reachable via `NodeChange(..)`. |
| `NodeTypes` | Renamed `NodeTypesMap` in PSFlow (see extras). |
| `ReactFlowActions` | PS models the store's action half as constructors of `React.Store.Action.Action` — a sum type a pure reducer folds — rather than as a record of methods, so there is no record for this name to denote. Documented in src/React.purs's header. Intentional and final. |
| `ResizeControlLineProps` | Props of upstream's internal `ResizeControlLine`. PS has no such component: `nodeResizeControl` serves both variants off one `ResizeControlProps`, selected by its `variant` field, so a second props record would describe nothing that exists — documented in src/React.purs's header. Intentional and final. |

**PSFlow-only, intentional:**

| Symbol | Rationale |
|--------|-----------|
| `ControlsProps` | PS rename of upstream `ControlProps`. |
| `EdgeTypesMap` | PS rename of upstream `EdgeTypes`. |
| `NodeTypesMap` | PS rename of upstream `NodeTypes`. |

## Shape parity (gates CI)

`typeof`, arity and React wrapper kind of every value `index.js` publishes,
compared against the vendored upstream's. Deliberately **not** scoped to the
boundary manifest's crossed set: a gate bounded by the conversion plan is
invisible-by-construction outside it, and the class that would have hidden
longest — component wrapper kind, boundary stage 4 at the earliest — is the one
this comparison rediscovered ticket 27 from.

Sharp edge, accepted: `Function.length` ignores default and rest parameters,
so an arity difference is occasionally a legitimate difference in how the two
sides declare their parameters rather than a currying gap. Those entries are
permanent rather than transitional.

- compared: 69 · same shape: 59 · differ: 10

### ❌ Un-allowlisted shape divergence (gates CI)

| Symbol | upstream | PSFlow |
|--------|----------|--------|
| _none_ | | |

### 📋 Known shape divergences (allowlisted)

Each entry names the event that retires it, so a boundary stage's definition of
done is mechanical: delete that stage's entries and this gate must stay green.

| Symbol | upstream | PSFlow | Retired by |
|--------|----------|--------|------------|
| `ReactFlow` | `forwardRef(fn/2)` | `fn/1` | Upstream is a `forwardRef`, PSFlow a plain function — `ReactFlow` is not a drop-in for upstream's because it does not accept a `ref`. This is ticket #27, which a ~20-line scratch script rediscovered independently. Retired by #27. |
| `ReactFlowWithRef` | `absent` | `forwardRef(fn/2)` | PSFlow-only, so upstream has no shape to agree with: it is the ref-taking wrapper that exists precisely because `ReactFlow` is not one. Retired by #27, which makes `ReactFlow` a drop-in and removes the reason for a second component. |
| `getConnectedEdges` | `fn/2` | `fn/1` | Curried: upstream takes (nodes, edges) in one call, PSFlow one argument at a time. Retired by boundary stage 5, which ticket 082 owns: #62 found that stage 4's `edgeTypes` makes this reachable — a custom edge component's first line calls one of these — and recorded that the fifth stage is warranted. |
| `getIncomers` | `fn/3` | `fn/1` | Curried: upstream (node, nodes, edges), PSFlow one at a time. Retired by boundary stage 5, which ticket 082 owns: #62 found that stage 4's `edgeTypes` makes this reachable — a custom edge component's first line calls one of these — and recorded that the fifth stage is warranted. |
| `getOutgoers` | `fn/3` | `fn/1` | Curried: upstream (node, nodes, edges), PSFlow one at a time. Retired by boundary stage 5, which ticket 082 owns: #62 found that stage 4's `edgeTypes` makes this reachable — a custom edge component's first line calls one of these — and recorded that the fifth stage is warranted. |
| `getViewportForBounds` | `fn/6` | `fn/1` | Curried: upstream takes six arguments, PSFlow one at a time. Retired by boundary stage 5, which ticket 082 owns: #62 found that stage 4's `edgeTypes` makes this reachable — a custom edge component's first line calls one of these — and recorded that the fifth stage is warranted. |
| `reconnectEdge` | `fn/3` | `fn/1` | Curried: upstream (oldEdge, newConnection, edges, options), PSFlow one at a time. Retired by boundary stage 5, which ticket 082 owns: #62 found that stage 4's `edgeTypes` makes this reachable — a custom edge component's first line calls one of these — and recorded that the fifth stage is warranted. |
| `useKeyPress` | `fn/0` | `fn/2` | Arity only, and a `Function.length` artifact rather than a divergence: upstream's `(keyCode = null, options = {})` both have defaults, so it reports 0 where the crossed ps-flow hook reports the 2 parameters it actually takes. Boundary stage 3 (#56) crossed it; matching the 0 would mean hand-writing an `arguments`-reading wrapper per hook, which makes the function lie about its shape to gain agreement with a number. **No boundary stage retires this** — the finding is recorded on #22, and it ends only if ps-flow gives these parameters JavaScript defaults of their own. |
| `useNodeConnections` | `fn/0` | `fn/1` | As `useKeyPress`: upstream destructures its single parameter with a `= {}` default, so arity 0 is a `Function.length` artifact and not a nullary declaration. Crossed in boundary stage 3 (#56); **no stage retires this**, recorded on #22. |
| `useNodesInitialized` | `fn/0` | `fn/1` | As `useKeyPress`: upstream's `options` parameter has a default, so it reports arity 0. Crossed in boundary stage 3 (#56); **no stage retires this**, recorded on #22. |

## Value behavior parity (gates CI)

The eight enum objects must be frozen and deep-equal upstream. The seventeen
pure functions are then called through `index.js` with the same inputs as the
vendored upstream bundle. A difference is keyed by both export and class, so an
allowlisted currying gap cannot later hide a different return-value regression.

- enum objects compared: 8 · differ: 0
- pure functions called: 17 · agree: 5 · differ: 12

### ❌ Un-allowlisted enum divergence (gates CI)

| Export | Difference class | upstream | PSFlow |
|--------|------------------|----------|--------|
| _none_ | | | |

### ❌ Un-allowlisted pure-function divergence (gates CI)

| Export | Difference class | upstream | PSFlow |
|--------|------------------|----------|--------|
| _none_ | | | |

### 📋 Known behavioral divergences (allowlisted)

Every entry names its retiring boundary stage. Fixing only part of a divergence
changes its class, which makes the old entry stale and the new class unclaimed.

| Export | Difference class | upstream | PSFlow | Retired by |
|--------|------------------|----------|--------|------------|
| `getBezierEdgeCenter` | `positional-array-return` | returns [75,60,75,40] | returns {"centerX":75,"centerY":60,"offsetX":75,"offsetY":40} | Returns a labelled record where upstream returns a positional array. Retired by boundary stage 5, which ticket 082 owns. |
| `getBezierPath` | `throw-vs-return` | returns ["M0,20 C75,20 75,100 150,100",75,60,75,40] | throws Error: Failed pattern match at System.Utils.Edges.Bezier (line 80, column 45 - line 84, column 69): String | Still consumes PureScript Position constructors, so the shared JS enum strings a custom edge component is handed throw before its labelled-record return is reached. One of the six that are actively broken from JavaScript rather than merely differently shaped. Retired by boundary stage 5, which ticket 082 owns. |
| `getConnectedEdges` | `call-convention` | returns [{"id":"source-focus","type":"<undefined>","source":"source","target":"focus","sourceHandle":"<undefined>","targetHandle":"<undefined>","animated":false,"hidden":false,"… | returns Function(arity=1) | Returns the next curried function instead of accepting both arrays in one call. Retired by boundary stage 5, which ticket 082 owns. |
| `getEdgeCenter` | `positional-array-return` | returns [75,60,75,40] | returns {"centerX":75,"centerY":60,"offsetX":75,"offsetY":40} | Returns a labelled record where upstream returns a positional array. Retired by boundary stage 5, which ticket 082 owns. |
| `getIncomers` | `call-convention` | returns [{"id":"source","position":{"x":0,"y":20},"data":{"label":"source"},"type":"<undefined>","sourcePosition":"<undefined>","targetPosition":"<undefined>","hidden":false,"se… | returns Function(arity=1) | Returns the next curried function instead of accepting node, nodes and edges together. Retired by boundary stage 5, which ticket 082 owns. |
| `getNodesBounds` | `call-convention` | returns {"x":0,"y":20,"width":240,"height":100} | returns Function(arity=1) | Its first call returns a function for PSFlow-only lookup/origin arguments where upstream returns the bounds. Retired by boundary stage 5, which ticket 082 owns. |
| `getOutgoers` | `call-convention` | returns [{"id":"target","position":{"x":200,"y":100},"data":{"label":"target"},"type":"<undefined>","sourcePosition":"<undefined>","targetPosition":"<undefined>","hidden":false,… | returns Function(arity=1) | Returns the next curried function instead of accepting node, nodes and edges together. Retired by boundary stage 5, which ticket 082 owns. |
| `getSimpleBezierPath` | `positional-array-return` | returns ["M0,20 C0,60 150,60 150,100",75,60,75,40] | returns {"path":"M0,20 C0,60 150,60 150,100","labelX":75,"labelY":60,"offsetX":75,"offsetY":40} | Returns a labelled record where upstream returns a positional array. Retired by boundary stage 5, which ticket 082 owns. |
| `getSmoothStepPath` | `throw-vs-return` | returns ["M0 20L20 20L 70,20Q 75,20 75,25L 75,95Q 75,100 80,100L130 100L150 100",75,60,75,40] | throws Error: Failed pattern match at System.Utils.Edges.SmoothStep (line 39, column 19 - line 43, column 34): String | Still consumes PureScript Position constructors, so the shared JS enum strings a custom edge component is handed throw before its labelled-record return is reached. One of the six that are actively broken from JavaScript rather than merely differently shaped. Retired by boundary stage 5, which ticket 082 owns. |
| `getStraightPath` | `positional-array-return` | returns ["M 0,20L 150,100",75,60,75,40] | returns {"path":"M 0,20L 150,100","labelX":75,"labelY":60,"offsetX":75,"offsetY":40} | Returns a labelled record where upstream returns a positional array. Retired by boundary stage 5, which ticket 082 owns. |
| `getViewportForBounds` | `call-convention` | returns {"x":300,"y":250,"zoom":2} | returns Function(arity=1) | Returns the next curried function instead of accepting all six upstream arguments together. Retired by boundary stage 5, which ticket 082 owns. |
| `reconnectEdge` | `call-convention` | returns [{"type":"<undefined>","source":"source","target":"target","sourceHandle":null,"targetHandle":null,"animated":false,"hidden":false,"deletable":"<undefined>","selectable"… | returns Function(arity=1) | Returns the next curried function instead of accepting the old edge, connection, edges and options together. Retired by boundary stage 5, which ticket 082 owns. |

## Prop-member parity (gates CI)

Members are restricted to those declared in the xyflow sources (inherited React
DOM attributes are excluded on both sides). Divergence must be declared in
`allowlist.json` under `props.<Type>` — as a `rename` (an upstream member
PSFlow surfaces under another name, cancelled on both sides by one entry) or as
a `missing`/`extra` entry with a rationale. Anything else fails the gate. The
`xPos`/`yPos` → `positionAbsoluteX`/`positionAbsoluteY` rename that this
section once merely printed was closed by ticket 069.

A rename is live only while all three of its premises hold — upstream still has
the name it translates from, PSFlow still has the name it translates to, and
PSFlow does not also publish upstream's own name. A rename that cancels nothing
is stale, and a rename standing in front of a member PSFlow has since added
under upstream's own name would otherwise hide a genuine extra member.

**Limit of this comparison: it is name-only.** Neither `upstream.json` nor
`psflow.json` carries member *types*, so a member whose type or arity changed
upstream while keeping its name still passes. That is an accepted limit, not an
oversight — making it type-aware needs a PureScript type printer, since today's
PS extraction is a brace-depth scan over `type X = { … }` synonyms. Tracked
separately; do not read a passing prop gate as a guarantee of type parity.

### ReactFlowProps

- upstream members: 122, PSFlow members: 124
- **missing in PSFlow** (0): _none_
- **extra in PSFlow** (0): _none_
- allowlisted extra: `children` _(React children are an explicit record field in PureScript rather than an implicit JSX slot; upstream gets them from React.PropsWithChildren, which the TS extractor does not attribute to the xyflow sources.)_, `onScroll` _(Inherited by upstream from HTMLAttributes<HTMLDivElement>, which the extractor excludes on both sides by design; PSFlow must declare it explicitly to expose it at all.)_

### NodeProps

- upstream members: 18, PSFlow members: 18
- **missing in PSFlow** (0): _none_
- **extra in PSFlow** (0): _none_

### EdgeProps

- upstream members: 28, PSFlow members: 28
- **missing in PSFlow** (0): _none_
- **extra in PSFlow** (0): _none_
